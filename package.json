{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild src/script.js --minify --format=esm --bundle --outdir=demo_widget/static"
	},
	"dependencies": {},
	"devDependencies": {
		"esbuild": "^0.24.0"
	}
}
